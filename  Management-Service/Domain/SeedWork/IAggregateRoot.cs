namespace ManagementService.Domain.SeedWork;

public interface IAggregateRoot
{
    // This is a marker interface to identify aggregate roots.
    // It does not define any members itself but serves to distinguish
    // aggregate root entities from other entities in the domain model.
}